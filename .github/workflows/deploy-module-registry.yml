name: Deploy Module Registry

on:
  push:
    branches:
      - hostinger-static
    paths:
      - 'admin/module-registry-v2.js'
      - 'admin/interface.js'
      - 'admin/auth-flow.js'
      - 'supabase/migrations/**'
      - '.github/workflows/deploy-module-registry.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-module-registry
  cancel-in-progress: false

jobs:
  migrate:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          ref: hostinger-static

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Validate production target and canonical migration
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          set -euo pipefail
          test -n "$SUPABASE_ACCESS_TOKEN"
          test -n "$SUPABASE_PROJECT_REF"
          test -n "$SUPABASE_DB_PASSWORD"
          test "$SUPABASE_PROJECT_REF" = "lwpiqshyqzsgwejvmbyo"
          test -f supabase/migrations/202607270003_normalize_combination_deals_module_key.sql
          test -f supabase/migrations/202607270004_verify_canonical_module_registry.sql
          grep -q "'combination_deals'" supabase/migrations/202607270004_verify_canonical_module_registry.sql

      - name: Apply and verify Module Registry migrations
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          set -euo pipefail
          supabase link --project-ref "$SUPABASE_PROJECT_REF" --password "$SUPABASE_DB_PASSWORD"
          supabase db push --include-all --password "$SUPABASE_DB_PASSWORD"

      - name: Deployment summary
        run: |
          echo '## Module Registry verified' >> "$GITHUB_STEP_SUMMARY"
          echo '- Four canonical platform modules enforced in Supabase' >> "$GITHUB_STEP_SUMMARY"
          echo '- Combination Deals uses module_key combination_deals' >> "$GITHUB_STEP_SUMMARY"
          echo '- Deployment fails when canonical verification fails' >> "$GITHUB_STEP_SUMMARY"