name: Supabase FitCoins Setup
on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: 'Type APPLY to install or update FitCoins & FitKado'
        required: true
        type: string
jobs:
  apply-rewards:
    runs-on: ubuntu-latest
    steps:
      - name: Validate confirmation
        if: ${{ inputs.confirmation != 'APPLY' }}
        run: exit 1
      - uses: actions/checkout@v4
      - name: Apply FitCoins database
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          test -n "$SUPABASE_ACCESS_TOKEN"
          test -n "$SUPABASE_PROJECT_REF"
          payload="$(jq -n --rawfile query supabase/fitconnect-rewards.sql '{query:$query}')"
          curl --fail-with-body --silent --show-error \
            -X POST "https://api.supabase.com/v1/projects/${SUPABASE_PROJECT_REF}/database/query" \
            -H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}" \
            -H "Content-Type: application/json" \
            --data "$payload"
      - name: Summary
        run: echo '## FitCoins & FitKado database is ready' >> "$GITHUB_STEP_SUMMARY"
