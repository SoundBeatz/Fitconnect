(()=>{const path=location.pathname.replace(/\/+$/,'')||'/';const depth=path.split('/').filter(Boolean).length;const root=depth===0?'./':depth===1?'../':'../../';const active=path==='/home'||path==='/'?'home':path.startsWith('/shop')?'shop':path.startsWith('/configurator')?'configurator':'home';const header=document.querySelector('header.nav-wrap,header.shop-nav,header.product-nav,header[data-fc-public-nav]');if(!header)return;const cartTile=active==='shop'?'<button class="fc-nav-tile fc-cart-tile" id="cartButton" type="button">Winkelmand <span class="fc-cart-count" id="cartCount">0</span></button>':'';header.className='fc-public-nav';header.setAttribute('data-fc-public-nav','');header.innerHTML=`<a class="fc-brand" href="${root}"><span class="fc-brand-mark">FC</span><span>FitConnect</span></a><button class="fc-nav-mobile-toggle" type="button" aria-expanded="false" aria-label="Menu openen">☰</button><nav class="fc-nav-grid is-collapsed" aria-label="Hoofdnavigatie"><a class="fc-nav-tile ${active==='home'?'active':''}" href="${root}">Home</a><a class="fc-nav-tile ${active==='shop'?'active':''}" href="${root}shop/">Shop</a><a class="fc-nav-tile ${active==='configurator'?'active':''}" href="${root}configurator/">Homegym ontwerpen</a><a class="fc-nav-tile fc-nav-muted" href="${root}#expertise">Over FitConnect</a>${cartTile}</nav>`;const toggle=header.querySelector('.fc-nav-mobile-toggle'),nav=header.querySelector('.fc-nav-grid');const sync=()=>{if(innerWidth>1180){nav.classList.remove('is-collapsed');toggle.setAttribute('aria-expanded','true')}else if(toggle.getAttribute('aria-expanded')!=='true'){nav.classList.add('is-collapsed')}};toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.classList.toggle('is-collapsed',open)});addEventListener('resize',sync);sync()})();