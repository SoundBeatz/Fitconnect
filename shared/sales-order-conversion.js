(()=>{'use strict';async function fromQuote(quote,{warehouse_id=null}={}){if(!quote?.id)throw new Error('Offerte ontbreekt');if(quote.status!=='accepted')throw new Error('Alleen geaccepteerde offertes kunnen worden omgezet');const prepared=window.FitConnectQuoteConversion?.prepare?.(quote)||quote;const payload={organization_id:quote.organization_id,workspace_id:quote.workspace_id,customer_id:quote.customer_id,contact_id:quote.contact_id,source_quote_id:quote.id,warehouse_id,currency:quote.currency,status:'draft',notes:quote.notes||'',metadata:{source:'quote',quote_number:quote.quote_number},lines:(prepared.lines||quote.lines||[]).map(l=>({...l,warehouse_id:l.warehouse_id||warehouse_id}))};const order=await window.FitConnectSalesOrders.create(payload);window.FitConnectRegistry?.emit?.('sales-orders:converted-from-quote',{quote_id:quote.id,sales_order_id:order.id});return order}window.FitConnectSalesOrderConversion={fromQuote};window.FitConnectRegistry?.register('sales-orders.conversion',window.FitConnectSalesOrderConversion,{meta:{type:'domain-service',version:'1.0.0',dependencies:['quotes']}})})();