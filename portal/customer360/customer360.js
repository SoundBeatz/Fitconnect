(()=>{'use strict';const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const euro=v=>new Intl.NumberFormat('nl-NL',{style:'currency',currency:'EUR'}).format(Number(v||0));async function boot(){const status=document.getElementById('status');if(!window.supabase||!window.FITCONNECT_SUPABASE){status.textContent='Portalconfiguratie ontbreekt.';return}const client=window.supabase.createClient(window.FITCONNECT_SUPABASE.url,window.FITCONNECT_SUPABASE.anonKey);const {data:{session}}=await client.auth.getSession();if(!session){location.replace('../../login/?next=/portal/customer360/');return}document.getElementById('logoutButton').onclick=async()=>{await client.auth.signOut();location.replace('../../login/')};const uid=session.user.id;const [m,d,w,l,s]=await Promise.all([client.from('customer_communications').select('subject,body,created_at').eq('customer_user_id',uid).eq('visible_to_customer',true).order('created_at',{ascending:false}).limit(20),client.from('customer_documents').select('title,document_type,external_url,created_at').eq('customer_user_id',uid).eq('visible_to_customer',true).order('created_at',{ascending:false}).limit(20),client.from('customer_wallet_balances').select('balance').eq('customer_user_id',uid).maybeSingle(),client.from('customer_wallet_ledger').select('entry_type,amount,description,created_at').eq('customer_user_id',uid).order('created_at',{ascending:false}).limit(10),client.from('customer_subscriptions').select('status,current_period_end,customer_subscription_plans(name,billing_interval,credits_per_cycle)').eq('customer_user_id',uid).order('created_at',{ascending:false}).limit(5)]);messages.innerHTML=m.error?'Berichten konden niet worden geladen.':m.data.length?m.data.map(x=>`<div class="c360-item"><strong>${esc(x.subject||'Bericht van FitConnect')}</strong><p>${esc(x.body)}</p></div>`).join(''):'<p class="c360-muted">Nog geen berichten.</p>';documents.innerHTML=d.error?'Documenten konden niet worden geladen.':d.data.length?d.data.map(x=>`<div class="c360-item"><strong>${esc(x.title)}</strong><p>${esc(x.document_type)}</p>${x.external_url?`<a href="${esc(x.external_url)}" target="_blank" rel="noopener">Open document</a>`:''}</div>`).join(''):'<p class="c360-muted">Nog geen documenten.</p>';wallet.innerHTML=`<div class="c360-balance">${euro(w.data?.balance||0)}</div><p class="c360-muted">Beschikbaar FitConnect-tegoed</p>${(l.data||[]).map(x=>`<div class="c360-item"><strong>${Number(x.amount)>0?'+':''}${euro(x.amount)}</strong> · ${esc(x.description||x.entry_type)}</div>`).join('')}`;subscriptions.innerHTML=s.error?'Abonnement kon niet worden geladen.':s.data.length?s.data.map(x=>`<div class="c360-item"><strong>${esc(x.customer_subscription_plans?.name||'FitConnect abonnement')}</strong><p><span class="c360-pill">${esc(x.status)}</span></p></div>`).join(''):'<p class="c360-muted">Geen actief abonnement.</p>';status.textContent='Dossier bijgewerkt.'}boot().catch(()=>{document.getElementById('status').textContent='Dossier kon niet volledig worden geladen.'})})();